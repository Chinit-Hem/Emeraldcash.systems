# Copy this file to android/keystore.properties. Never commit the copied file.
# The values below are examples only; replace them with your own secure values.
storeFile=release/emeraldcash-release.jks
storePassword=REPLACE_WITH_KEYSTORE_PASSWORD
keyAlias=emeraldcash
keyPassword=REPLACE_WITH_KEY_PASSWORD
